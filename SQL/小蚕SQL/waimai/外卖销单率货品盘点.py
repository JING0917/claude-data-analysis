
# 一、分析目标再确认（非常关键）

### 本次分析的唯一目标：

> **以「销单率 = 有效订单量 / 活动名额」为核心指标，
> 在活动 ID 维度下，通过多维度盘货，定位“哪些活动的有效订单表现有问题”。**

⚠️ 注意：

* **不是直接下结论“原因是什么”**
* 而是回答：

  * **有没有问题**
  * **问题集中在哪些活动 / 哪类活动**
  * **下一步应该重点分析什么方向**

---

# 二、数据预处理与样本说明（必须先说清楚）

## 1️⃣ 关于「销单率 > 1」的处理原则

### 业务解释（你已经给出，我这里规范化表达）：

* 销单率 > 1 的原因：

  * 活动名额在商家差评、风控等情况下被系统释放
  * 用户可在“释放名额”下继续下单
* 因此：

  * **销单率 > 1 属于业务合理现象**
  * 但 **不适合用于“名额是否卖得好”这一判断**

---

## 2️⃣ 是否排除「销单率 > 1」活动？

### 我的建议（给你一个专业、稳妥的做法）：

> **在“定位销单率低的问题活动”这一分析中，排除销单率 > 1 的活动。**

### 原因说明（可直接写进分析文档）：

* 销单率 > 1 的活动：

  * 已经不存在“名额是否卖完”的问题
  * 会 **干扰对低销单率活动的分布判断**
* 本次分析的关注点是：

  * **有效订单是否显著低于活动名额**
* 因此：

  * 将销单率 > 1 作为 **“正常售罄或超额售罄活动”单独剥离**

### 在分析中需要明确给出的说明（模板）：

> 本次分析中，为聚焦“活动名额是否有效转化为订单”，
> 对销单率 > 1 的活动进行剥离。
> 被剥离活动共 X 个，占比 X%，对应活动名额总量 X。
> 该类活动在业务上属于正常售罄或超售，不纳入“销单不足”问题定位范围。

（你后续只需补数）

---

# 三、分析维度设计（核心）

在**活动 ID 已经是最细粒度**的前提下，盘货一定要从「活动属性」和「流量-转化结构」入手。

### 我建议你至少从以下 5 大维度展开（你不需要全用，但这是完整方案）

---

## 维度一：活动名额规模（最重要的第一刀）

### 为什么先看这个？

> **很多“销单率低”的问题，本质是“名额定得不合理”。**

### 分析方法：

1. 按活动名额分桶，例如：

   * ≤10
   * 11–30
   * 31–50
   * 51–100
   * > 100
2. 在每个名额区间内，做描述性统计：

   * 活动数
   * 平均 / 中位销单率
   * 销单率分布（P25 / P50 / P75）

### 你要重点找的信号：

* **名额越大，销单率系统性下降**
* 某些名额区间：

  * 销单率集中 < 0.3 / 0.5

👉 如果成立，说明：

> **问题不在“下单能力”，而在“名额配置策略”**

---

## 维度二：曝光量分布（流量是否配得上名额）

### 关键判断逻辑：

> **低销单率 ≠ 转化差，可能只是“没流量”。**

### 描述性统计要做什么：

在同一维度下，看：

* 曝光量分布（均值 / 中位数 / 长尾）
* 有效订单量与曝光的相关性
* 曝光 / 名额 比值

### 你要重点关注：

* **曝光极低，但名额较高的活动**
* 曝光与有效订单高度正相关，但销单率仍低的活动

👉 这类活动优先标记为：

> **流量供给不足型活动**

---

## 维度三：活动类型 / 活动机制（如果文件中有）

例如：

* 秒杀 / 返现 / 团购 / 折扣
* 是否限时
* 是否新客专享

### 分析方法：

* 在同一活动类型下：

  * 看销单率分布
  * 看名额与有效订单的匹配程度

### 典型可定位问题：

* 某类活动：

  * 曝光不低
  * 但销单率系统性偏低

👉 这通常指向：

> **机制设计或用户心智问题**

---

## 维度四：商家 / 城市 / 区域（如果有）

### 核心问题：

> **是不是问题集中在“某些供给侧”？**

### 你要看：

* 同一城市 / 商家下：

  * 多个活动是否反复低销单
* 销单率在区域维度的离散程度

👉 如果成立：

> **问题不是活动，而是商家质量或区域需求**

---

## 维度五：活动生命周期（如果有开始/结束时间）

### 非常容易被忽略，但价值很高：

* 是否存在：

  * 新活动普遍低销单
  * 老活动更容易卖完

👉 这可以直接为：

> **活动冷启动机制分析** 提供方向

---

# 四、盘货时必须做的描述性统计（你可以直接照着做）

在**每一个维度下**，你都要至少回答这 4 个问题：

1. 活动名额分布是否极不均衡？
2. 有效订单量是否存在明显长尾？
3. 曝光与有效订单的相关性是否显著？
4. 低销单率活动是否集中出现？

这是一个**标准的盘货 checklist**。

---

# 五、如何“定义销单率低”的问题活动（关键）

### 推荐你使用“相对定义”，而不是拍脑袋阈值：

例如：

* 在同维度内：

  * 销单率 **低于 P25**
  * 或 **低于均值 - 1σ**
* 且：

  * 曝光不在最低 20%

👉 这样的活动，可以定义为：

> **“有流量但未转化的潜在问题活动”**

这是后续深挖原因的**黄金样本**。

---

# 六、最终你应该输出什么（符合你当前阶段目标）

在当前阶段，**你不需要给原因**，只需要给方向：

### 你最终要拿到的 3 类结果：

1. **销单率低活动的集中区间**

   * 集中在哪些名额段
   * 集中在哪些活动类型
2. **明显异常的活动集合**

   * 有曝光、有名额、但有效订单显著偏低
3. **后续分析优先级**

   * 先看名额策略？
   * 先看流量分配？
   * 还是先看活动机制？

---

## 最后一句总结（给你自己 & 管理层）

> 当前阶段的分析重点不是“为什么卖不掉”，
> 而是 **“哪些活动本身就不该被卖这么多名额”**，
> 以及 **“哪些活动明明有条件卖，但结果没卖好”**。

如果你愿意，下一步我可以直接帮你：

* **设计一套 SQL / Python 盘货模板**
* 或 **帮你定义“问题活动白名单/黑名单规则”**
* 或 **把这套逻辑整理成数分复盘 PPT 结构**

你只需要告诉我：
👉 **“下一步做哪一个”**


###########################################
# 活动销单率盘货分析

目标：
- 以「销单率 = 有效订单量 / 活动名额」为核心指标
- 在活动 ID 粒度下，通过多维度盘货
- 定位销单率偏低的活动，为后续原因分析提供方向

说明：
- 销单率 > 1 的活动属于业务正常现象（名额释放、超售）
- 本分析中将该类活动剔除，仅聚焦“名额未有效转化”的问题活动


import pandas as pd
import numpy as np
import matplotlib.pyplot as plt

plt.rcParams["font.sans-serif"] = ["Arial Unicode MS"]  # mac 中文
plt.rcParams["axes.unicode_minus"] = False


## 读取数据 基础校验
file_path = "20260120_下单转化_大禾.xlsx"
df = pd.read_excel(file_path)

print("数据行数：", len(df))
print("字段数：", df.shape[1])
df.head()


## 指标校验&衍生指标
# 防止分母为 0
df["活动名额"] = df["活动名额"].replace(0, np.nan)

# 再算一次销单率用于校验
df["销单率_cal"] = df["有效订单量"] / df["活动名额"]

# 曝光 / 名额（判断流量是否匹配）
df["曝光_名额比"] = df["首页活动曝光量"] / df["活动名额"]

# 首页点击率
df["首页CTR"] = df["首页活动点击量"] / df["首页活动曝光量"].replace(0, np.nan)

df[["活动ID", "活动名额", "有效订单量", "销单率", "销单率_cal"]].head()


## 剔除销单率>1的数据
df_over1 = df[df["是否销单率大于1"] == "是"]
df_valid = df[df["是否销单率大于1"] != "是"].copy()

print("原始活动数：", len(df))
print("销单率 > 1 的活动数：", len(df_over1))
print("剔除后分析活动数：", len(df_valid))
print("被剔除活动的名额总量：", df_over1["活动名额"].sum())

# 活动名额分布和销单率
bins = [0, 10, 30, 50, 100, 1000]
labels = ["≤10", "11-30", "31-50", "51-100", ">100"]

df_valid["名额区间"] = pd.cut(df_valid["活动名额"], bins=bins, labels=labels)

quota_stats = (
    df_valid
    .groupby("名额区间")
    .agg(
        活动数=("活动ID", "count"),
        平均名额=("活动名额", "mean"),
        平均销单率=("销单率", "mean"),
        中位销单率=("销单率", "median"),
        P25销单率=("销单率", lambda x: x.quantile(0.25))
    )
    .reset_index()
)

quota_stats

# 名额VS销单率 这张图是“名额是否合理”的核心证据图
plt.figure(figsize=(8,4))
plt.bar(quota_stats["名额区间"], quota_stats["中位销单率"])
plt.title("不同活动名额区间的中位销单率")
plt.xlabel("活动名额区间")
plt.ylabel("中位销单率")
plt.show()


# 曝光、订单、销单率的相关性（判断是不是流量问题）
# 汇报时重点解读：曝光 vs 有效订单  名额 vs 销单率
corr_cols = [
    "活动名额",
    "首页活动曝光量",
    "首页活动点击量",
    "有效订单量",
    "销单率"
]

corr_matrix = df_valid[corr_cols].corr()
corr_matrix


# 定义“问题活动”（关键）
# 定义原则（相对而非拍脑袋）：
# 销单率 < 全体 P25
# 且曝光不在最低 20%
sell_rate_p25 = df_valid["销单率"].quantile(0.25)
expo_p20 = df_valid["首页活动曝光量"].quantile(0.20)

problem_df = df_valid[
    (df_valid["销单率"] < sell_rate_p25) &
    (df_valid["首页活动曝光量"] > expo_p20)
].copy()

print("销单率 P25：", round(sell_rate_p25, 3))
print("曝光量 P20：", int(expo_p20))
print("识别出的问题活动数：", len(problem_df))


# 问题活动画像（盘货结论核心）
# 按一级品类、二级品类
problem_profile = (
    problem_df
    .groupby("二级品类")
    .agg(
        活动数=("活动ID", "count"),
        平均名额=("活动名额", "mean"),
        平均曝光=("首页活动曝光量", "mean"),
        平均有效订单=("有效订单量", "mean"),
        平均销单率=("销单率", "mean")
    )
    .sort_values("活动数", ascending=False)
)

problem_profile


# 输出问题活动明细（用于复盘 & 下钻）
output_cols = [
    "统计日期", "活动ID",
    "城市名称", "区县名称",
    "一级品类", "二级品类",
    "活动名额",
    "首页活动曝光量",
    "有效订单量",
    "销单率"
]

problem_detail = problem_df[output_cols].sort_values("销单率")
problem_detail.head(20)

















