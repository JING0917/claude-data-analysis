# 数据架构演进

## 现状：StarRocks 被当成全能层

```
        ┌──────────────┐
        │   业务数据源   │
        └──────┬───────┘
               │ 写入
               ▼
    ┌──────────────────────┐
    │                      │
    │   StarRocks 存算一体   │
    │        (一套)         │
    │                      │
    └──┬─────────┬────┬────┘
       │         │    │
       ▼         ▼    ▼
   ┌──────┐ ┌──────┐ ┌──────────┐
   │ 看板  │ │ 报表  │ │ 后端服务   │ ← OLTP 点查也走这
   └──────┘ └──────┘ └──────────┘
                          │
                       3秒超时
```

StarRocks 同时承接 OLAP（分析）+ OLTP（点查）+ 存储，后端服务直接查 StarRocks 时延迟不可控。

### 存算一体在腾讯云上的隔离困境

腾讯云托管版 StarRocks 存算一体架构下，BE 节点承载全部存储和计算任务，资源隔离存在先天限制：

**Workload Group 只能做软隔离。** 它把 CPU 和内存按 query 分组划分，但磁盘 I/O 和网络带宽是所有 Workload Group 共享的，无法隔离。后端点查扫热数据时吃 I/O，分析侧大查询 `GROUP BY` 全表扫描也吃 I/O——两股流量在同一个 BE 进程里抢同一块盘，谁也躲不开。

**扩容绑死。** 想给分析侧加算力就必须同时加存储（每扩一个 BE 都带一块盘），想扩存储容量也得捎上计算——成本不是线性增长的，是按节点整体翻倍的。业务高峰期分析查询和点查并发上去，只能整体升配，没法单独给某一层加弹性。

**实践验证。** 此前试图用「再搭一套 StarRocks 集群给后端专用」来缓解，结果是：存储数据要写两份，两套集群各自维护，隔离效果有限（单集群内部的 I/O 竞争依然存在），本质上是用冗余成本换一个不彻底的解法。

## 目标：Paimon + StarRocks 存算分离 + MySQL/Redis

```
                     ┌──────────────┐
                     │   业务数据源   │
                     └──────┬───────┘
                            │ 实时写入 (CDC / Kafka)
                            ▼
              ┌──────────────────────────────┐
              │         OSS / COS            │ ← 共享存储
              │                              │
              └──┬──────────────┬────────────┘
                 │              │
        Paimon 管理元数据   StarRocks 计算层
        ACID / Upsert       只做分析查询
        Time Travel         弹性扩缩
                 │              │
                 ▼              ▼
        ┌──────────────┐ ┌──────────────┐
        │  Paimon 外表  │ │  物化视图     │
        │  (实时数仓)   │ │  (预计算)     │
        └──────────────┘ └──────┬───────┘
                                │
                        定时同步 / ETL
                                │
                     ┌──────────┴──────────┐
                     ▼                     ▼
               ┌──────────┐          ┌──────────┐
               │  MySQL    │          │  Redis    │
               │  持久化   │          │  热缓存   │
               └────┬─────┘          └────┬─────┘
                    │                     │
                    └──────────┬──────────┘
                               │
                               ▼
                        ┌──────────────┐
                        │   后端服务     │  ← 点查 + 高频聚合
                        │  < 50ms      │
                        └──────┬───────┘
                               │
                         ┌─────┴──────┐
                         ▼            ▼
                    ┌──────┐     ┌──────┐
                    │ 看板  │     │ 报表  │
                    └──────┘     └──────┘
```

## 各层职责

| 层 | 角色 |
|----|------|
| **Paimon** | 湖格式，管实时写入、Upsert、ACID、Time Travel。数据存 OSS/COS，计算引擎可插拔 |
| **StarRocks** | 只做分析查询。挂载 Paimon 外表，存算分离模式弹性扩缩，不再管写入和点查 |
| **MySQL** | OLTP 持久化层。后端点查的最终出口，数据从 StarRocks 物化视图定时同步 |
| **Redis** | 热缓存层。高频指标预计算后缓存，后端读缓存过期再穿透到 MySQL |
| **后端服务** | 只访问 MySQL/Redis，延迟可控 |

## 跟现状的核心区别

| 现状 | 改造后 |
|------|--------|
| StarRocks 扛写入 | Paimon 扛写入 |
| StarRocks 扛点查 | MySQL/Redis 扛点查 |
| 一套集群不分家 | 分析和服务分层 |
| 锁在存算一体，扩缩绑死 | 存算分离，计算层弹性扩缩，存储层按量付费 |
| Workload Group 软隔离，磁盘 I/O 竞争不可控 | 各层物理隔离，I/O 路径完全分开 |
| 后端 3 秒超时 | 后端 50ms 以内 |
| 尝试两套集群双写，冗余成本高 | 统一存储底座，无数据冗余 |
